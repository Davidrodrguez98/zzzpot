## Copy to `.env` for local overrides (do not commit `.env`)
## Spring reads these env vars automatically.

# PostgreSQL
DB_URL=jdbc:postgresql://localhost:5432/mydatabase
DB_USERNAME=myuser
DB_PASSWORD=secret

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

